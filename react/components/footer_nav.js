/**
 * The function FooterNav is a React component that renders a navigation element with children as its
 * content.
 * @returns A `FooterNav` functional component is being returned. It takes a `children` prop and
 * renders a `nav` element.
 */
export default function FooterNav({children}){
    return(
        <nav>
            
        </nav>
    )
}
